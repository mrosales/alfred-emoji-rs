[package]
name = "xtask"
description = "Dataset generation and icon rendering tasks (see PLAN.md)"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
publish = false

[dependencies]
anyhow = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ureq = "2"

[lints]
workspace = true
